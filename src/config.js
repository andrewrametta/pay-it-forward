const config = {
  PORT: process.env.PORT || 8000,
  TOKEN_KEY: "pay-it-foward-token",
  API_ENDPOINT: "http://localhost:8800",
  // "https://fathomless-dawn-52857.herokuapp.com" || "http://localhost:8800",
};

export default config;
